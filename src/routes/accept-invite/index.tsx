// FR-02-03 · SC-019 — accept a shared-class colleague invitation. Owns its own top-level route,
// mounted at `/accept-invite` (same pattern as `/sign-in/reset?token=…`) — PUBLIC, pre-login.
export { AcceptInviteScreen as AcceptInviteApp } from "./AcceptInviteScreen"
